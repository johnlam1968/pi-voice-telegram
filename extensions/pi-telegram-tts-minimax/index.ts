/**
 * pi-telegram-tts-minimax — STUB.
 *
 * TODO (next session): implement the MiniMax T2A + ffmpeg libopus pipeline
 * ported from the current `pi-voice-telegram/synthesis-provider.ts`,
 * `voice-reply.ts`, and `mm-tts.ts`. Register as a synthesis provider
 * via `registerTelegramVoiceSynthesisProvider` with a stable id
 * (`pi-telegram-tts-minimax/tts`). Add a section in
 * /telegram-settings for the TTS controls (voice, lang, model, verify).
 *
 * Settings in telegram.json under `extensions["pi-telegram-tts-minimax"]`.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function piTelegramTtsMinimax(pi: ExtensionAPI): void {
	// TODO: implement
	void pi;
}
